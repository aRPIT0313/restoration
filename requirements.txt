pip install google-generativeai    # Gemini API
pip install rembg                  # background removal
pip install ftfy regex tqdm        # CLIP dependencies
pip install git+https://github.com/openai/CLIP.git
pip install faiss-cpu              # similarity search
pip install langchain              # agent orchestration
pip install streamlit              # UI
pip install Pillow requests        # utilities
pip install python-dotenv